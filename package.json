{
  "name": "@atrium-ui/components",
  "version": "0.0.4",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "turbo lint",
    "build": "turbo build",
    "publish:preview": "npm pack && tar -xvzf *.tgz && rm -rf package *.tgz"
  },
  "workspaces": [
    "apps/*",
    "components/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
