---
title: Button
---

import DefaultButtonVueRaw from "./button/default.vue?raw";
import DefaultButtonVue from "./button/default.vue";
import OutlineButtonVueRaw from "./button/outline.vue?raw";
import OutlineButtonVue from "./button/outline.vue";
import GhostButtonVueRaw from "./button/ghost.vue?raw";
import GhostButtonVue from "./button/ghost.vue";
import LinkVueRaw from "./button/link.vue?raw";
import LinkVue from "./button/link.vue";
import { Example, ComponentHeader } from "package:/components";

<ComponentHeader name="Button">
 A button.
</ComponentHeader>

<Example title="Default" code={DefaultButtonVueRaw}>
  <DefaultButtonVue client:only="vue" />
</Example>

<Example title="Outline" code={OutlineButtonVueRaw}>
  <OutlineButtonVue client:only="vue" />
</Example>

<Example title="Ghost" code={GhostButtonVueRaw}>
  <GhostButtonVue client:only="vue" />
</Example>

<br/>
<br/>

## Links

Links are not buttons and buttons are not links. Links have hrefs and buttons do not, so having props that apply only in a certain variation is confusing.

But sometimes you need a link that looks like a button. So we have a separate Link component with a button variant. You can replace the a tag inside this component with a framework specific router link.

<Example code={LinkVueRaw}>
  <LinkVue client:only="vue" />
</Example>
