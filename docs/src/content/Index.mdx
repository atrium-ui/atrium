---
title: Atrium UI
description: Copy-paste components you own. Framework-agnostic custom elements under the hood. No dependencies, no lock-in.
---

import { Command } from "../components";

<section className="flex flex-col items-center text-center gap-module-xl mb-module-8xl">
  <div className="pb-module-l">
    <img className="light-mode-only" width="120" src={`${import.meta.env.BASE_URL}atrium-light.png`} />
    <img className="dark-mode-only" width="120" src={`${import.meta.env.BASE_URL}atrium-dark.png`} />
  </div>

  <h1 className="typo-title-1 pb-module-s">Build Better, Ship Faster</h1>


  <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-1">
    <a className="home-card" href={`${import.meta.env.BASE_URL}components/accordion`}>
      <a-animation
        src={`${import.meta.env.BASE_URL}animation/atrium_components.riv`}
        autoplay
        height={200}
        width={320}
        loop
        className="w-full h-full"
        layout="contain"
        artboard="Accordion"
      ></a-animation>
      <span className="sr-only">Accordion</span>
    </a>
    <a className="home-card" href={`${import.meta.env.BASE_URL}components/dialog`}>
      <a-animation
        src={`${import.meta.env.BASE_URL}animation/atrium_components.riv`}
        autoplay
        height={200}
        width={320}
        loop
        className="w-full h-full"
        layout="contain"
        artboard="Dialog"
      ></a-animation>
      <span className="sr-only">Dialog</span>
    </a>
    <a className="home-card" href={`${import.meta.env.BASE_URL}components/popover`}>
      <a-animation
        src={`${import.meta.env.BASE_URL}animation/atrium_components.riv`}
        autoplay
        height={200}
        width={320}
        loop
        className="w-full h-full"
        layout="contain"
        artboard="Popover"
      ></a-animation>
      <span className="sr-only">Popover</span>
    </a>
    <a className="home-card" href={`${import.meta.env.BASE_URL}components/carousel`}>
      <a-animation
        src={`${import.meta.env.BASE_URL}animation/atrium_components.riv`}
        autoplay
        height={200}
        width={320}
        loop
        className="w-full h-full"
        layout="contain"
        artboard="Slider"
      ></a-animation>
      <span className="sr-only">Carousel</span>
    </a>
  </div>

  <p className="typo-body max-w-[600px] text-balance opacity-80">
    Copy-paste components that you own. Framework-agnostic custom elements under the hood.
    No dependencies, no lock-in, just clean code.
  </p>

  <div className="flex flex-wrap gap-module-m pt-module-xl justify-center">
    <a href={`${import.meta.env.BASE_URL}guides/setup`} className="inline-flex items-center gap-2 rounded-lg bg-(--style-typography-body) text-white px-6 py-3 font-medium hover:opacity-90 transition-opacity">
      Get Started
    </a>
    <a href={`${import.meta.env.BASE_URL}playground`} className="inline-flex items-center gap-2 rounded-lg border border-current px-6 py-3 font-medium hover:bg-(--style-typography-body) hover:text-white transition-colors">
      Try Playground
    </a>
  </div>
</section>

<section className="pb-module-8xl">
  <h2 className="typo-title-2 text-center pb-module-3xl">Why Atrium?</h2>

  <div className="grid grid-cols-1 md:grid-cols-2 gap-module-2xl lg:px-module-4xl">
    <div className="p-module-xl rounded-lg border border-(--style-typography-body)/10">
      <h3 className="typo-headline pb-element-xs">🎨 You Own the Code</h3>
      <p className="opacity-80">Components are copied into your project, not imported. Modify anything without waiting for upstream changes or fighting abstraction layers.</p>
    </div>
    <div className="p-module-xl rounded-lg border border-(--style-typography-body)/10">
      <h3 className="typo-headline pb-element-xs">⚡ Tiny Bundle Size</h3>
      <p className="opacity-80">Minimal Dependencies. The entire elements package has minimal dependencies.</p>
    </div>
    <div className="p-module-xl rounded-lg border border-(--style-typography-body)/10">
      <h3 className="typo-headline pb-element-xs">🔧 Framework Agnostic</h3>
      <p className="opacity-80">Works with Vue, React, Astro, or vanilla JS. Custom elements play nice with everything.</p>
    </div>
    <div className="p-module-xl rounded-lg border border-(--style-typography-body)/10">
      <h3 className="typo-headline pb-element-xs">♿ Accessible by Default</h3>
      <p className="opacity-80">Keyboard navigation, ARIA attributes, and focus management baked into every element. A11y isn't an afterthought.</p>
    </div>
  </div>
</section>

<section className="pb-module-8xl lg:px-module-4xl">
  <h2 className="typo-title-2 text-center pb-module-xl">Get Started in Seconds</h2>

  <div className="markdown-content max-w-[700px] mx-auto">
    <p className="text-center pb-module-xl opacity-80">Install the elements package and copy your first component:</p>

    <Command bun="bun add @sv/elements" />

    <br/>

    ```sh
    npx @sv/components button
    ```

    <p className="text-center pt-module-xl">
      <a href={`${import.meta.env.BASE_URL}guides/setup`} className="underline underline-offset-4 hover:opacity-80">
        Read the full setup guide →
      </a>
    </p>
  </div>
</section>

<section className="pb-module-6xl">
  <h2 className="typo-title-2 text-center pb-module-4xl">Two Layers, Maximum Flexibility</h2>

  <div className="markdown-content lg:px-module-4xl">
    <div className="grid grid-cols-1 md:grid-cols-2 gap-module-2xl pb-module-2xl">
      <div>
        <h3 className="typo-headline pb-element-xs">Components</h3>
        <p className="opacity-80">Pre-styled, ready-to-use templates built with TailwindCSS. Copy them into your project and make them yours.</p>
        <p className="pt-module-s">
          <a href={`${import.meta.env.BASE_URL}playground`} className="underline underline-offset-4 hover:opacity-80">Try Playground →</a>
        </p>
      </div>
      <div>
        <h3 className="typo-headline pb-element-xs">Elements</h3>
        <p className="opacity-80">Headless custom elements that handle behaviour and accessibility.</p>
        <p className="pt-module-s">
          <a href={`${import.meta.env.BASE_URL}elements/a-expandable`} className="underline underline-offset-4 hover:opacity-80">Browse Elements →</a>
        </p>
      </div>
    </div>
  </div>
</section>
