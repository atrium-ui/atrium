---
title: Sheet
---

import Sheet from "@sv/components/sheet.tsx";
import Button from "@sv/components/button.tsx";

```bash
npx @sv/mono use sheet
```

A sheet is basically a burger menu. Composed of a-blur.


<div class="not-content min-h-[300px] flex items-center justify-center">
  <Sheet client:load>
    <div onclick="document.querySelector('a-blur').enabled = false">
      <Button>Close</Button>
    </div>
  </Sheet>

  <div onclick="document.querySelector('a-blur').enabled = true">
    <Button>Open</Button>
  </div>
</div>

### Code
```tsx
<Sheet>
  <div onclick="document.querySelector('a-blur').enabled = false">
    <Button>Close</Button>
  </div>
</Sheet>

<div onclick="document.querySelector('a-blur').enabled = true">
  <Button>Open</Button>
</div>
```
