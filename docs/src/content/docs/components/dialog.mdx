---
title: Dialog
---
import Example from "../../../components/Example.astro";
import { Dialog } from "@sv/components/templates/dialog.tsx";
import { Button } from "@sv/components/templates/button.tsx";

```bash
npx @sv/components use dialog
```

A sheet is basically a burger menu. Composed of an a-blur.

<Example title="Example">
  <div slot="preview">
    <Dialog>
      ## Dialog
      This is a dialog.

      <div class="mt-4" onclick="document.querySelector('a-blur').enabled = false">
        <Button>Close</Button>
      </div>
    </Dialog>

    <div onclick="document.querySelector('a-blur').enabled = true">
      <Button>Open</Button>
    </div>
  </div>
  <div slot="code">
    ```tsx
      <Dialog>
        ## Dialog
        This is a dialog.

        <div class="mt-4" onclick="document.querySelector('a-blur').enabled = false">
          <Button>Close</Button>
        </div>
      </Dialog>

      <div onclick="document.querySelector('a-blur').enabled = true">
        <Button>Open</Button>
      </div>
    ```
  </div>
</Example>
