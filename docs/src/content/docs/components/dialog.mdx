---
title: Dialog
---

import Dialog from "@atrium-ui/components/dialog.tsx";
import Button from "@atrium-ui/components/button.tsx";

```bash
npx atrium use dialog
```

A sheet is basically a burger menu. Composed of a-blur.

<div class="not-content min-h-[300px] flex items-center justify-center">
  <Dialog client:load>
    ## Dialog
    This is a dialog.

    <div class="mt-4" onclick="document.querySelector('a-blur').enabled = false">
      <Button>Close</Button>
    </div>
  </Dialog>

  <div onclick="document.querySelector('a-blur').enabled = true">
    <Button>Open</Button>
  </div>
</div>


### Code
```tsx
<Dialog client:load>
  ## Dialog
  This is a dialog.

  <div onclick="document.querySelector('a-blur').enabled = false">
    <Button>Close</Button>
  </div>
</Dialog>

<div onclick="document.querySelector('a-blur').enabled = true">
  <Button>Open</Button>
</div>
```
