---
title: Button
---

import Example from "../../../components/Example.astro";
import Icon from "../../../components/Icon.astro";
import { Button, Link } from "@sv/components/src/vue/Button";

A button.

```bash
npx @sv/components button
```

<Example title="Example" code={`
---
import { Button } from "./Button";
---

<Button>Button</Button>
<Button disabled>Disabled</Button>
<Button>
  <Icon /><span>with icon</span>
</Button>
`}>
  <div class="flex gap-10">
    <Button>Button</Button>
    <Button disabled>Disabled</Button>
    <Button>
      <Icon /><span>with icon</span>
    </Button>
  </div>
</Example>

<Example title="Outline" code={`
---
import { Button } from "./Button";
---

<Button variant="outline">Button</Button>
<Button variant="outline" disabled>Disabled</Button>
<Button variant="outline">
  <span>with icon</span><Icon />
</Button>
`}>
  <div class="flex gap-10">
    <Button variant="outline">Button</Button>
    <Button variant="outline" disabled>Disabled</Button>
    <Button variant="outline">
      <span>with icon</span><Icon />
    </Button>
  </div>
</Example>

<Example title="Ghost" code={`
---
import { Button } from "./Button";
---

<Button variant="ghost">Button</Button>
<Button variant="ghost" disabled>Disabled</Button>
<Button variant="ghost">
  <Icon /><span>with icon</span>
</Button>
`}>
  <div class="flex gap-10">
    <Button variant="ghost">Button</Button>
    <Button variant="ghost" disabled>Disabled</Button>
    <Button variant="ghost">
      <Icon /><span>with icon</span>
    </Button>
  </div>
</Example>

<br/>
<br/>

## Links

Links are not buttons and buttons are not links. Links have hrefs and buttons do not, so having props that apply only in a certain variation is confusing.

But sometimes you need a link that looks like a button. So we have a separate Link component with a button variant. You can replace the a tag inside this component with a framework specific router link.

<Example code={`
---
import { Link } from "./Button";
---

<Link variant="default" href="https://google.com" target="_blank">To somewhere</Link>
<Link variant="outline" href="https://bing.com" target="_blank">To somewhere else</Link>
`}>
  <div class="flex gap-10 text-white">
    <Link variant="default" href="https://google.com" target="_blank">To somewhere</Link>
    <Link variant="outline" href="https://bing.com" target="_blank">To somewhere else</Link>
  </div>
</Example>
